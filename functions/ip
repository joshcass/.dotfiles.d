function ip() {
  if [ "$(ifconfig | grep 192.168)" ]; then
    local grep_ip="$(ifconfig | grep '192\.168')"
  else
    local grep_ip="$(ifconfig | grep '10\.0')"
  fi

  echo $grep_ip | awk '{print $2}' | tee >(pbcopy)
}
