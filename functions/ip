function ip() {
  if [ "$(ifconfig | grep 192)" ]; then
    local grep_ip="$(ifconfig | grep 192)"
  else
    local grep_ip="$(ifconfig | grep 10.0)"
  fi

  echo $grep_ip | awk '{print $2}' | tee >(pbcopy)
}
